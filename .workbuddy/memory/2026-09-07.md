# 2026-09-07

- v1.15.0（b29479a）：管理页打开任意 KV key 即全量加载（`limit=20000`），删除「加载全部」按钮与 30 条样本流程。用户明确：数据每次都全量获取，防止样本与全量出现差异。
- `last_refresh_at` 可读化：worker 新增 `fmtBeijing(ms)`（UTC+8），`handleAdminKvGet` 对 `LAST_REFRESH_KEY` 返回 `kind: "ts"` + `readable`；`handleAdminKvList` 的兜底分支给该 key 补 `updatedAt`。admin.html 概览卡片显示「X 分钟前 / X 小时前 / X 天前」，抽屉里显示可读时间 + 原始毫秒。
- 数组快照响应去掉重复的 `sample` 字段（`sample = arrLike ? null : ...`），响应体积减半。
- 验证：本地 esbuild 语义构建 + jsdom 冒烟全过（时间戳卡片「5 分钟前」、members_full 请求 `?limit=20000`、「共 609 行」、ts 详情可读时间）；线上已部署并确认 `/members`、`/admin.html` 200，部署后 HTML 中 `btnLoadAll` 已不存在。

- v1.16.0（8237540）：管理台布局重构为「左选表 · 中铺表 · 右看行」。用户诉求：中间大块空间应给整张表格，表格不该从右侧抽屉出来；抽屉只用于看某一行的详情。
  - 左栏 = 数据表选择器（4 张表：中文名 + KV key + 条数徽标 + 新鲜度圆点），首次进入自动选中第一张；顶栏加「最近刷新 · X 分钟前」胶囊；侧栏脚注放最近刷新时间 / 头像缓存数 / 数据源。
  - 中间主区 = 头部（表名 + key/体积/更新时间/条数 + 全表搜索 + 表格⇄JSON + 导出 + 重载）+ AG Grid 铺满剩余高度（`overflow:hidden` + flex 列，页面不滚动）。
  - 右侧抽屉 = 只做行详情：字段/JSON 双视图、上一条/下一条（←→ 或 j/k）、复制 JSON、Esc 关闭。
  - 删掉 KV 卡片网格总览（信息并入左栏列表与脚注）；`last_refresh_at` 与 `avatar_img_` 不再作为「表」出现在选择列表。
  - 坑：jsdom 里 `showRowDetail` 收到的是已展平行（`rowsOf()` 里 flattenRow 过），但为兜底在 `showRowDetail` 入口再 `flattenRow` 一次。
  - **重要：本项目部署 = git push 触发 CF 自动部署，不要用 `wrangler deploy`**（会因多账号无法选择而失败，且会绕过用户的部署流程）。
  - 验证：esbuild 语义构建 + jsdom 冒烟 20/20 通过（脚本 `/Users/shensunfeng/.workbuddy/binaries/node/workspace/smoke_admin.mjs`，需放在 workspace 内跑，ESM 不认 NODE_PATH）。

- v1.16.1（9d03e67）：表格日期数字可读化（纯前端，数据不动）。飞书日期字段实际存法（实测 members_full）：
  - 毫秒时间戳：修改时间 / 录入时间 / 入社日期 / 生日（1.5e12~1.8e12）
  - Excel 日期序列：**倒数日** 46023~46387 = 2026-01-01~2026-12-31（即"今年生日"之类，不是倒计时天数）
  - 普通数字（勿误伤）：参与活动时长 0~4996、志愿服务时长 0~888、参与活动次数 0~71
  - 判定法：`n>=1e12 && n<=4e12` 直接按 ms 转（量级足够明确，不依赖字段名）；秒级(1e9~4e9)与 Excel 序列(20000~80000)必须字段名命中 `日期|时间|生日|倒数日|截止|期限|date|time|At$` 才转。
  - 纯日期判定别用 `n % 86400000 === 0`（UTC 0 点才成立，北京 0 点的入社日期 1536681600000 会漏判）→ 改为对 +8 小时后的 Date 判断 h/m/s 全 0。
  - valueGetter 也返回可读串（让排序/过滤/CSV 导出都为可读日期，`YYYY-MM-DD HH:mm` 字典序即时间序）；tooltip 附原始值；抽屉里可读日期 + 灰色原始值并列。
  - 冒烟扩到 33 项全过。
