# 2026-09-06 工作日志

## 树莓社数据流全景图 + 文档全面核对（shumeinfc-projects）
- 对 README.md / CHANGELOG.md / docs/activity-api-plan.md / AGENTS.md 与 worker.js @ ba1377f 逐行核对，产出 `docs/dataflow-audit-2026-09-06.html`（全景图 + 差异清单，44KB 单文件）。
- 核心结论（文档普遍滞后 2 个功能批次）：
  1. README 接口清单缺 5 条：/api/activities、/api/activities/detail、/api/photo（09-03 活动功能）、/api/proof-files、/api/file（09-06 证明功能）。
  2. README KV key 表只列 3/6：缺 activity_records_v3、activity_projects_v1、file_catalog_v1；目录快照 file_catalog_v1 惰性 60min + cron/refresh force。
  3. 版本史与 CHANGELOG 停在 1.9.0（2026-08-26），09 月两批功能无版本条目。
  4. README 称「不启用 Image Resizing」已过时：/api/photo 实际用 cf.image 缩放（w∈[200,400,800,1200]），activity 方案八节实测已开启。
  5. worker.js 头注释漏列 GET /api/members/detail 一行。
  6. AGENTS.md 仍为扣子托管/COZE_WEBHOOK 时代描述，已过时。
- 核对一致：activity-api-plan.md 与实现吻合（无 activity_detail_v1、详情无 participants、白名单隔离均落地）；api-baseline 自洽。
- 事实参考：文件表 tblO5pPurRqVPMR5、detail 新增 joinDate 字段（09-06，供微信云函数 proofs 资格判定：发布时间>=入社日期）、owner 字段。

## 同日修订 v2：dataflow-audit-2026-09-06.html 接口案例 + 格式
- 用户反馈后修订：① 全文档加 13 个接口的「案例速查」折叠区（每个接口配请求形态+响应 JSON 结构，值全部脱敏，结构照实，素材来自 docs/api-baseline 真实抓包）；② 案例按接口卡顺序重排并编号 §3-1~12，卡片内加锚点链接互跳；③ 格式修复：body min-width 1100 + overflow-x、表格外包 .table-scroll、endpoint/URL word-break、代码块深色横向滚动。
- 经验：该 HTML 文件每次写入/预览会被系统注入 data-page-node-id 属性 → 无法用精确字符串 Edit，改用 Python 正则（容错属性）批处理；过程中曾误将 <div class="node 前缀拼到文件头，用栈检查发现未闭合 div 后修复（145/145 平衡、12 details/12 链接全部对齐）。

## 同日：全部说明文档整合更新为最新（对应 worker.js @ ba1377f）
- README.md 整篇重写：13 接口全表（补 activities×2/photo/proof-files/file）、KV 6 key 表+刷新方式、附件三端点对比表（avatar L2永久 / photo cf.image / file 原样）、Image Resizing 改为「已开启」、版本表补 1.10.0(09-03)/1.11.0(09-06)、项目结构补 docs/、新增「活动数据与社员证明（小程序链路）」节、维护节加文档三件套同步义务。
- CHANGELOG.md 补 1.10.0（活动 API + activity_projects_v1 + 头像不再回退个人照片）与 1.11.0（proof-files/file + file_catalog_v1 + joinDate + owner）两条。
- AGENTS.md 整篇重写：从扣子托管时代描述改为当前 Cloudflare Worker 架构（13 接口速览、4 飞书表 ID、开发部署、约定与坑：q 查无 500 是既有行为勿修、字段名逐字符匹配、api-baseline 禁止外发等）。
- docs/activity-api-plan.md 头部加 09-06 复核注（P1-P5 全部完成上线，作为权威实现说明）；worker.js 头注释补 GET /api/members/detail 一行。
- .wranglerignore 追加 docs/dataflow-audit-*.html（审查交付物不入线上静态资源）。
- 未动：DESIGN.md（纯 UI 规范仍适用）、docs/api-baseline/README.md（冻结历史快照）。

## 同日：运维操作台分析报告 docs/admin-console-plan.md
- 结论：可行，基础设施已具备 ~80%（Worker 内建 KV API、refresh 鉴权/冷却先例、4 快照构建函数现成、自定义域名同域部署）。
- 推荐形态：worker.js 新增 /api/admin/* 端点组（新 Secret ADMIN_TOKEN，与 REFRESH_TOKEN 隔离）+ 根目录单文件 admin.html（登录/总览/KV 查看/一键刷新/开关面板）。
- 三阶段：P0 骨架+summary（半天）→ P1 KV 查看器+scope 刷新+v1 页面（1~2 天）→ P2 flags_v1 开关框架 + maintenance/checkinOpen/detailPublic 三开关（~1 天）。
- 关键决策：KV 查看默认脱敏（登录密码等打码，reveal 二次确认，后端双重防绕过）；开关判断须在 Cache API 缓存命中前（fetch 路由层）；不做"改飞书数据"入口（写权威入口唯一）；README 公开/管理 API 分两表，版本记 1.12.0。

## 同日：admin-console-plan.md 更新 v2（用户澄清需求后极简版）
- 用户澄清：①看KV=只读；②一键取数=直接触发现有 POST /api/refresh（每次全量防差异）；③"数据开关"=**字段级隐私开关**（如某社员「升学去向」关掉 → detail/member 输出 ""，与没填一致），不是全局功能开关；且不改动 KV。
- v2 方案：开关=飞书社员表加勾选列（「升学去向不公开」「详细介绍不公开」，复用「禁止查询」模式，加一列+一行过滤代码），Worker buildDetailMember/buildMember 输出端过滤；一键刷新复用现有 refresh（页面持 REFRESH_TOKEN 即可，Worker 零改动）；KV 只读端点 2 个（脱敏+reveal）；总预估 ≤1.5 天。
- 待用户确认：members_full（App 快照）是否豁免过滤（建议豁免，与「禁止查询」先例一致）；档位 A（飞书勾选）/B（管理页开关面板+写飞书端点）。

## 同日：admin-console-plan.md v3（用户再次澄清：网页中间开关层）
- 用户否决飞书加列（字段太多），要"网页上点、中间开关"；members_full 豁免已确认。
- v3 关键分析：网页开关必须有持久化位置，现实候选只有 飞书列（否决）或 KV 新增小 key；KV 只是"储物柜"不算"改 KV"（不动现有 6 key、不提供直接改 KV 的 UI）。
- v3 方案：开关 key `field_hidden_v1`（{识别码:[grad,bio,…], updatedAt}），仅 /api/members/detail 输出过滤（置 "" 与没填一致）；保存时 cache.delete 该社员 detail 缓存使秒级生效；内存 30s 缓存读开关零 KV 压力；members/member/full/avatar 等全豁免。
- 管理端点：GET/POST /api/admin/field-visibility + KV 只读 list/get（脱敏 reveal）+ refresh 鉴权兼容 ADMIN_TOKEN（1 行）；admin.html 登录+KV 一览+一键刷新+开关面板；合计 ≤2 天，试点字段建议 grad/bio/honor/alias。
- 待确认 3 细节：首发字段清单、独立 ADMIN_TOKEN、选人交互。

## 同日：实现「看 KV + 一键全量刷新」（v1.12.0，管理台 P0+P1 先行）
- worker.js 新增管理端点（128 行纯增量）：GET /api/admin/kv/list（6 key 状态：条数/体积/updatedAt/健康 + avatar 前缀仅计数）、GET /api/admin/kv?key=&reveal=1（内容样本：SENSITIVE_FIELDS=[登录密码,生日,年龄] 默认打码、数组截前 30 条/60KB、avatar_img 图片仅回字节数）、POST /api/admin/refresh（转 handleRefresh）；鉴权 adminAuthed=Bearer ADMIN_TOKEN，handleRefresh 鉴权兼容 REFRESH_TOKEN 或 ADMIN_TOKEN；管理响应一律 no-store、不加 CORS（仅同源可调）。
- 新增根目录 admin.html（单文件、无框架、浅色风格沿用 index.html）：登录口令存 sessionStorage（用 kv/list 探测有效性）→ Tab1 KV 状态表（点击行看脱敏样本、reveal 二次 confirm）+ Tab2 一键全量刷新（显示 4 快照条数、30s 冷却倒计时、成功后联动刷新 KV 列表）。token 展示打码（前6…后4）。
- README/CHANGELOG 记 v1.12.0：README 接口清单加「管理 API」小节 + 部署表加 ADMIN_TOKEN + 部署节加 CF Zero Trust Access 可选加固说明 + 项目结构补 admin.html；CHANGELOG 补 1.12.0 条目；worker.js 头注释补管理端点。
- 安全结论（答复用户"用 CF 权限那套吗"）：先应用层 Bearer ADMIN_TOKEN（免费立即可用），CF Access（Zero Trust）作为可选叠加——把 /admin.html 与 /api/admin/* 包 CF 登录墙，README 已写配置路径。
- 验证：node --check worker.js + admin 内联 JS 均过；脱敏/截断逻辑 Node 模拟验证 ✓。
- 部署提醒：新增 Secret ADMIN_TOKEN 需在 CF Dashboard 手动添加；admin.html 会被 assets 部署为公开路径（页面本身无数据，接口有鉴权，安全）。

## 同日：v1.12.0 回归检查（c44aa24 vs ba1377f，用户已 commit+push）
- 方法：git 双版本逐函数体指纹比对（花括号级提取）+ 路由冲突检查 + 语法检查。
- 结论：13 个公开 handler + scheduled + 工具函数**全部函数体逐字节一致**；唯一改动 handleRefresh 为有意扩展（REFRESH_TOKEN 或 ADMIN_TOKEN 均可，语义向后兼容：两者都未配时仍 401）；路由新增 /api/admin/* 三条与既有 API/静态资源零冲突（顺序 kv/list → kv → refresh，均在 ASSETS 之前）。
- 顺带修复：docs/dataflow-diagram.html 此前漏加 .wranglerignore 排除（会随部署公开），已补；audit HTML 中含 2 个真实附件 token（TBSobr…、Bqs7b…，来自基线样例），已被排除部署，但已进 git 历史——仓库若公开需注意。该修复需再次 commit+push 才生效。
- 教训：Bash 内 grep 对 \b 转义异常返回 0（误报标识符缺失），用 Grep 工具复核确认无冲突。

## 同日：admin.html 升级为"数据库浏览器"表格视图（用户反馈 JSON 可读性差）
- worker.js handleAdminKvGet 重构：返回结构化 data 对象（前端渲染用）+ truncated 标志；截断改为 slice 前 30 条（不再往数组 push 字符串说明，避免污染表格行）；data 受 60000 字符上限保护（截断后 data=null 自动回退 JSON 文本视图）。
- admin.html 详情区新增「表格 / JSON」视图切换：动态列收集（优先排 recordId/id/name/memberCode/title 主键）、行=记录列=字段、冻结首列、横向滚动、单元格截断 + hover title 全量、数组/对象显示 [n]/ {n} 摘要、顶部任意值筛选（150ms 防抖）、点击行展开单条 key-value 详情（可看完整嵌套 JSON）、大表提示"样本前 N 条/共 M 条"。
- 支持两种数据形态：{recordId, fields:{34列}}（members_full）与平铺对象（activity_records_v3 等）；非 items 结构 key 自动落 JSON 视图。
- 验证：node --check 双文件通过；Node 模拟 flatten/动态列/筛选逻辑 ✓（members 5 列、records 6 列符合预期）。
- 注意：admin.html/worker.js 每次被预览系统注入 data-page-node-id → Write 前必须先 Read。

## 同日：按用户要求去掉"显示敏感值"机制，KV 管理页原文直出
- 用户决策：内部授权工具不需要脱敏，直接显示原文。
- worker.js：删除 SENSITIVE_FIELDS/sanitizeValue/reveal 参数（+~15 行删除）；handleAdminKvGet 返回 data=j 原文，响应字段去掉 redacted；头注释同步。
- admin.html：移除「显示敏感值」按钮、revealTip 提示条、currentReveal 状态、confirm 逻辑；说明文案改为"内容原文直出（内部授权工具）"。
- README.md 同步 4 处：管理 API 说明/表格行/安全备注/管理台安全说明 → 原文直出口径；v1.12.0 版本描述去掉"脱敏样本"。
- 校验：node --check worker.js + admin 内联 JS、HTML 标签配平、零残留关键字（btnReveal/revealTip/currentReveal/redacted/reveal=1）。
- 安全注意保留项：ADMIN_TOKEN 鉴权、no-store、无 CORS、样本截前 30 条仍在（仅防整库倒出的传输体积）。
- 部署故障（23:30）：CF 构建报 "Cannot assign to j because it is a constant" worker.js:1372（表格化重构时 `const j = JSON.parse(raw)` 分支里 `j = j.slice(...)`）。**教训：node --check 只查语法、查不出对 const 赋值，需用 esbuild 语义构建复现 CF 环境**（本地 node_modules workspace 有 esbuild，`node_modules/.bin/esbuild worker.js --bundle --format=esm` 与 CF 构建器一致）。修复（let j）+ esbuild 验证 + commit c700d7f 推送后部署成功。线上验证：/api/admin/kv/list 无 token 401、/api/members 200、/admin 页面为表格新版（flattenRow/rawData 在、btnReveal 无）。另：/admin.html 307 → /admin（Workers Static Assets 干净 URL 默认行为，正常）。

